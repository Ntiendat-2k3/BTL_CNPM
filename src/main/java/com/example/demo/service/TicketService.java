package com.example.demo.service;

import org.springframework.stereotype.Service;

@Service
public class TicketService {
    public byte[] generateTicketPdf(String movieName, String time, String seat, String date, String price) {
        // Thêm code để tạo file PDF
        return new byte[0];  // Test với mảng rỗng trước
    }
}
